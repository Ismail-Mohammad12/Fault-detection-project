# fault-detection-project
In industry 4.0, AI plays a huge role in identifying the faults in the machinery which results in reduced costs and downtime. In this project, an XGBoost model has been trained to predict the failure, trained on the past records of machine data.
